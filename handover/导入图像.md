# 导入图像 Import Image 交接文档

## 1. 概述

**类名**: `SkybrushHHImportImageOperator`
**文件路径**: `src/modules/sbstudio/plugin/operators/import_image.py`

**功能**: 将图像文件转换为无人机编队的位置点云。算法通过分析图像的灰度分布，自动提取前景/背景区域，将**背景区域**（即图像的非主体部分）转换为无人机的空间位置。支持**形态膨胀**和**自适应圆点检测**两种模式，适用于不同的图像类型。

---

## 2. 类属性与参数

### 2.1 Operator 属性

| 属性 | 值 |
|------|-----|
| `bl_idname` | `export_scene.import_image` |
| `bl_label` | Import Image |
| `bl_options` | `REGISTER`（不支持撤销） |
| 基类 | `Operator`, `ImportHelper` |

### 2.2 参数说明

| 参数 | 类型 | 默认值 | 范围 | 说明 |
|------|------|--------|------|------|
| `filepath` | StringProperty | - | 文件路径 | 要导入的图像文件路径（由 ImportHelper 提供文件选择器） |
| `filter_glob` | StringProperty | 所有图像格式 | - | 文件过滤器，包含 Blender 支持的所有图像格式（`bpy.path.extensions_image`） |
| `min_distance` | FloatProperty | 3.0m | ≥0.1m | 导入后无人机之间的最小间距 |
| `threshold` | FloatProperty | 1.0 | ≥0.01 | 背景颜色阈值（基于灰度标准差的倍数） |
| `structure` | EnumProperty | DIR8 | DIR4/DIR8 | 膨胀结构方向数（4方向或8方向） |
| `iterations` | IntProperty | 1 | ≥0 | 膨胀迭代次数 |
| `adaptive` | BoolProperty | False | - | 是否使用自适应圆点检测算法 |

---

## 3. 核心算法逻辑

### 3.1 执行流程总览

```
invoke() → 弹出文件选择器
    ↓
execute() → 加载图像并处理
    ↓
1. 加载图像，提取灰度通道
2. 基于中位数 + 标准差计算背景阈值
3. 生成二值掩码（True = 背景区域）
    ↓
┌─ adaptive=False（膨胀模式）:
│  1. 迭代膨胀前景区域
│  2. 提取连通区域质心作为无人机位置
│
└─ adaptive=True（自适应模式）:
   1. 连通块标记 + 面积中位数估算圆点直径
   2. 距离变换 + 高斯平滑
   3. 局部极大值检测圆点中心
   4. 坐标后处理去重
    ↓
4. 计算点云最小间距，按比例缩放至 min_distance
5. 创建网格对象（存储无人机位置）
6. 创建图像参考对象（Empty，显示原始图像）
7. 将参考对象放入"图片"集合
```

### 3.2 灰度分析与背景提取

```python
pixels = np.asarray(image.pixels, dtype=np.float32).reshape(width * height, 4)
img_gray = pixels[:, :3].mean(axis=1, dtype=np.float32)           # RGB → 灰度
base, threshold = np.median(img_gray), img_gray.std() * self.threshold
lower, upper = base - threshold, base + threshold
pixels = np.logical_and(img_gray > lower, img_gray < upper).reshape(height, width)
```

**算法原理**:
1. 将 RGBA 像素转为灰度（RGB 三通道均值）
2. 计算灰度中位数 `base` 和标准差 `threshold`
3. 灰度在 `[base - threshold, base + threshold]` 范围内的像素标记为**背景**（`True`）
4. 其余像素标记为**前景**（`False`）

**直观理解**: 假设图像主体是白色文字、黑色背景。中位数可能是黑色（背景色），标准差反映灰度变化范围。白色文字的灰度远超 `base + threshold`，因此不会被标记为背景。

### 3.3 膨胀模式（非自适应）

#### 形态膨胀 [`execute` 第83-92行](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/operators/import_image.py#L83-L92)

```python
for _ in range(self.iterations):
    padded = np.pad(pixels, 1, mode='edge')    # 边界填充，防止越界
    neighbors = np.zeros_like(pixels, dtype=np.bool_)
    neighbors |= padded[1:-1, 0:-2] | padded[1:-1, 2:]    # 左 + 右
    neighbors |= padded[0:-2, 1:-1] | padded[2:, 1:-1]    # 上 + 下
    if self.structure == "DIR8":
        neighbors |= padded[0:-2, 0:-2] | padded[0:-2, 2:] | \
                     padded[2:, 0:-2] | padded[2:, 2:]     # 对角线
    pixels |= neighbors
```

**膨胀操作**: 将前景像素的邻居也标记为前景。相当于"扩大"前景区域。

| 方向 | 邻居位置 | 代码 |
|------|----------|------|
| 4方向 | 左、右、上、下 | `padded[1:-1, 0:-2]` 等 |
| 8方向 | 4方向 + 左上、右上、左下、右下 | `padded[0:-2, 0:-2]` 等 |

**膨胀效果**: 前景区域每迭代一次扩大一圈。多次迭代后，原本分离的前景区域会合并成更大的连通块。

#### 质心提取 [`centroid` 方法](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/operators/import_image.py#L124-L140)

```python
@staticmethod
def centroid(pixels, x, y):
    height, width = pixels.shape
    points = []
    def scan(points, x, y):
        P, X = [], x + 1
        while x >= 0 and not pixels[y, x]:
            P.append(x); pixels[y, x], x = True, x - 1   # 向左扫描，标记已访问
        while X < width and not pixels[y, X]:
            P.append(X); pixels[y, X], X = True, X + 1   # 向右扫描，标记已访问
        if y > 0:
            [pixels[y - 1, x] or scan(points, x, y - 1) for x in P]   # 向上递归
        if y + 1 < height:
            [pixels[y + 1, x] or scan(points, x, y + 1) for x in P]   # 向下递归
        points += [(x, y) for x in P]
    scan(points, x, y)
    return np.mean(points, axis=0) + 0.5
```

**算法**: 从前景像素 `(x, y)` 出发，使用**深度优先搜索**遍历整个连通的前景区域，计算区域中心（质心）。

**工作流程**:
1. 从种子像素向左、右扫描，收集同一行中所有连续的前景像素
2. 对每个收集到的像素，递归检查上、下行的相邻像素
3. 标记已访问的像素（`pixels[y, x] = True`），避免重复遍历
4. 返回连通区域的平均坐标 + 0.5（像素中心偏移）

**调用方式**:
```python
points = np.array([self.centroid(pixels, x, y)
    for y in range(height) for x in range(width) if not pixels[y, x]])
```

遍历所有前景像素，每个连通区域只处理一次（因为扫描过程中会标记已访问）。

### 3.4 自适应圆点检测

#### 检测流程 [`detect_dots_adaptive` 方法](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/operators/import_image.py#L142-L185)

```python
@staticmethod
def detect_dots_adaptive(ndimage, binary):
    # 1. 连通块标记
    label_im, nb_labels = ndimage.label(binary)
    
    # 2. 估算圆点直径（面积中位数法）
    sizes = ndimage.sum(binary, label_im, range(1, nb_labels + 1))
    avg_area = np.median(sizes)
    estimated_d = 2 * np.sqrt(avg_area / np.pi)
    
    # 3. 距离变换 + 高斯平滑
    distance = ndimage.distance_transform_edt(binary)
    sigma_val = estimated_d / 8
    smoothed_dist = ndimage.gaussian_filter(distance, sigma=sigma_val)
    
    # 4. 局部极大值检测
    filter_size = int(estimated_d / 3)
    local_max = ndimage.maximum_filter(smoothed_dist, size=filter_size) == smoothed_dist
    local_max = local_max & (smoothed_dist > estimated_d * 0.2)
    
    # 5. 坐标去重
    return filter_close_points(raw_centers, min_sep=estimated_d / 3)
```

**适用场景**: 图像中包含**离散的圆形标记**（如圆点、十字、光斑），每个圆点需要映射为一架无人机的位置。

#### 步骤详解

| 步骤 | 方法 | 作用 |
|------|------|------|
| 1. 连通块标记 | `ndimage.label` | 将二值图像中的连通区域编号，得到标记矩阵和标签数量 |
| 2. 直径估算 | `ndimage.sum` + `median` | 计算每个连通块的面积，取中位数。使用中位数可以排除重叠块（面积过大）和噪点（面积过小）的干扰。`d = 2√(area/π)` |
| 3. 距离变换 | `distance_transform_edt` | 计算每个前景像素到最近背景像素的欧氏距离。圆心的距离值最大。 |
| 4. 高斯平滑 | `gaussian_filter` | 平滑距离图，消除圆点内部的局部波动，使峰值更明显。`sigma ≈ d/8` |
| 5. 局部极大值 | `maximum_filter` | 在每个 `filter_size ≈ d/3` 的窗口内找到距离最大值，即为圆点中心 |
| 6. 坐标去重 | `filter_close_points` | 贪婪去重：依次取点，移除所有距离太近的点。`min_sep ≈ d/3` |

#### 距离变换示意图

```
原始圆点 (二值):          距离变换后:
   █████                   0 0 0 0 0 0 0
   █   █                   0 1 2 3 2 1 0
   █ O █       →           0 2 4 5 4 2 0
   █   █                   0 1 2 3 2 1 0
   █████                   0 0 0 0 0 0 0
                               ↑ 最大值=圆心
```

### 3.5 点云缩放

```python
if len(points) > 1:
    dist_matrix = np.linalg.norm(points[:, np.newaxis] - points, axis=2)
    np.fill_diagonal(dist_matrix, np.inf)
    scale = self.min_distance / np.min(dist_matrix)
else:
    scale = 1.0
```

**缩放原理**:
1. 计算点云中所有点对之间的欧氏距离矩阵
2. 对角线（自身距离）设为无穷大，避免影响最小值
3. 找到最小间距 `min_dist`
4. 缩放因子 `scale = min_distance / min_dist`

**效果**: 缩放后，点云中最接近的两架无人机的间距恰好等于 `min_distance`。

### 3.6 网格与图像对象创建

#### 网格对象（无人机位置）

```python
mesh = bpy.data.meshes.new("mesh_" + filename)
mesh.vertices.add(len(points))
mesh.vertices.foreach_set("co", np.insert(points * scale, 1, 0, axis=1).flatten())
bpy.context.scene.collection.objects.link(bpy.data.objects.new(filename, mesh))
```

- 创建空网格，顶点数 = 点数
- 将 2D 图像坐标 `(x, y)` 转为 3D 坐标 `(x, 0, y)`（插入 Z=0）
- `foreach_set` 批量设置顶点坐标，性能优于逐个赋值

#### 图像参考对象（Empty）

```python
empty = bpy.data.objects.new("image_" + filename, None)
empty.empty_display_type = 'IMAGE'
empty.data = image
empty.empty_image_offset = (0, 0)
empty.use_empty_image_alpha = True
empty.color[3] = 0.25                    # 25% 透明度
empty.rotation_euler[0] = math.pi / 2   # 绕 X 轴旋转 90°，使图像水平放置
empty.scale = [max(width, height) * scale] * 3
```

在 3D 视图中显示原始图像作为参考，方便对照查看无人机位置。

#### "图片"集合管理

```python
collection = bpy.data.collections.get("图片")
if collection is None:
    collection = bpy.data.collections.new("图片")
    bpy.context.scene.collection.children.link(collection)
collection.objects.link(empty)
```

将图像参考对象统一放入"图片"集合，便于管理和隐藏。

---

## 4. 数学模型

### 4.1 坐标映射

```
图像像素 (px, py) → 3D 空间 (px × scale, 0, py × scale)
```

- X 轴对应图像宽度方向
- Z 轴对应图像高度方向
- Y 轴固定为 0（水平地面）

### 4.2 距离变换

对于二值图像 `B(x, y)`：

```
D(x, y) = min{ ||(x,y) - (x',y')|| | B(x',y') = 0 }
```

即每个前景像素到最近背景像素的欧氏距离。

### 4.3 局部极大值检测

```
local_max(x, y) = (D_smoothed(x, y) == max{D_smoothed(u, v) | |u-x|≤size/2, |v-y|≤size/2})
```

在 `size × size` 的窗口内，如果当前像素的距离值最大，则认为是局部极大值。

---

## 5. 实际应用场景

### 5.1 场景一：文字编队

**需求**: 将"HELLO"文字图像转换为无人机编队。

| 配置 | 值 |
|------|-----|
| `adaptive` | False |
| `threshold` | 1.0 |
| `structure` | DIR8 |
| `iterations` | 3-5 |
| `min_distance` | 3m |

**效果**: 文字笔画（前景）被膨胀合并为连通块，每个笔画段的质心对应一架无人机。

### 5.2 场景二：圆点图案

**需求**: 图像中包含离散圆点标记，每个圆点对应一架无人机。

| 配置 | 值 |
|------|-----|
| `adaptive` | True |
| `threshold` | 1.0 |
| `min_distance` | 3m |

**效果**: 自适应检测每个圆点的精确中心，不受圆点大小不一或重叠的影响。

### 5.3 场景三：简单几何图形

**需求**: 将三角形、五角星等几何图形轮廓映射为无人机位置。

| 配置 | 值 |
|------|-----|
| `adaptive` | False |
| `iterations` | 0（不膨胀） |
| `min_distance` | 2m |

**效果**: 提取图形的连通区域质心，一架无人机对应一个完整图形。

---

## 6. 数据结构

### 6.1 输入

| 变量 | 类型 | 说明 |
|------|------|------|
| `filepath` | str | 图像文件路径 |
| `image.size` | (width, height) | 图像像素尺寸 |
| `image.pixels` | list[float] | RGBA 像素数组，长度 = width × height × 4 |

### 6.2 中间数据

| 变量 | 类型 | 形状 | 说明 |
|------|------|------|------|
| `pixels` (初始) | np.array | (W×H, 4) | 原始 RGBA 像素 |
| `img_gray` | np.array | (W×H,) | 灰度值（RGB 均值） |
| `pixels` (二值) | np.array | (H, W) | 背景掩码（True = 背景） |
| `distance` | np.array | (H, W) | 距离变换图 |
| `points` | np.array | (N, 2) | 提取的 2D 点坐标 |
| `dist_matrix` | np.array | (N, N) | 点对距离矩阵 |

### 6.3 输出

| 对象 | 类型 | 说明 |
|------|------|------|
| `mesh_<filename>` | Mesh | 包含所有无人机位置的网格对象 |
| `image_<filename>` | Empty (Image) | 原始图像参考对象 |
| `图片` | Collection | 存放所有图像参考对象的集合 |

---

## 7. 依赖关系

### 7.1 外部依赖

| 模块 | 用途 |
|------|------|
| `numpy` | 数组操作、距离矩阵、图像数据处理 |
| `scipy.ndimage`（可选） | 自适应模式的连通块标记、距离变换、高斯平滑、极大值检测 |

### 7.2 Blender API

| 模块 | 用途 |
|------|------|
| `bpy.data.images` | 加载图像文件 |
| `bpy.data.meshes` | 创建网格对象 |
| `bpy.data.collections` | 管理集合 |
| `bpy.path.extensions_image` | 获取支持的图像格式列表 |

---

## 8. 性能特征与限制

### 8.1 时间复杂度

| 操作 | 复杂度 | 说明 |
|------|--------|------|
| 灰度转换 | O(W×H) | 一次遍历所有像素 |
| 膨胀迭代 | O(K × W×H) | K 为迭代次数 |
| 质心提取 | O(W×H) | 每个像素最多被访问一次（标记机制） |
| 距离矩阵 | O(N²) | N 为点数 |
| 自适应检测 | O(W×H) | 基于 scipy 的高效实现 |

### 8.2 已知限制

| 限制 | 说明 |
|------|------|
| **2D 映射** | 仅生成平面（Y=0）上的位置，不支持 3D 图像导入 |
| **灰度依赖** | 基于 RGB 均值提取灰度，对彩色前景/背景可能不准确 |
| **膨胀模式精度** | 质心提取对不规则形状的连通区域可能产生偏差 |
| **自适应模式依赖 scipy** | 未安装 scipy 时无法使用自适应模式 |
| **不支持撤销** | `bl_options = {"REGISTER"}` 不包含 `UNDO` |
| **去重效率** | `filter_close_points` 使用 O(N²) 的贪婪策略，点数过多时较慢 |

---

## 9. 调试验证方法

1. 准备一张测试图像（如黑白文字或圆点图案）
2. 执行 `export_scene.import_image` 操作符
3. 选择图像文件，调整参数
4. 执行后检查：
   - 3D 视图中是否生成了网格对象
   - 是否创建了图像参考对象（Empty Image）
   - 网格顶点位置是否与图像前景区域对应
   - "图片"集合中是否包含参考图像

### 9.1 常见问题排查

| 问题 | 可能原因 |
|------|----------|
| 未生成任何点 | 图像全部为背景色，或 threshold 设置过小 |
| 点数过多 | threshold 设置过大，背景区域过小 |
| 点位置偏移 | 质心计算对不规则形状的偏差 |
| scipy 错误 | 未安装 scipy，无法使用自适应模式 |
| 图像不显示 | Empty 的 alpha 设置为 0.25，可能在某些视图中不可见 |

---

## 10. 常见问题 FAQ

### Q1: 为什么背景像素标记为 True，前景标记为 False？

算法的设计思路是：**背景是连续的、大面积的区域**，前景是分散的、小块的目标。将背景标记为 True 便于使用形态膨胀来"填充"前景区域之间的空隙，然后将填充后的区域分割为连通块。

### Q2: `threshold` 参数如何影响结果？

- `threshold` 越大，背景判定范围越宽，更多像素被标记为背景 → 前景区域越小 → 提取的点数越少
- `threshold` 越小，背景判定范围越窄，更多像素被标记为前景 → 前景区域越大 → 提取的点数越多

### Q3: 膨胀模式与自适应模式的区别？

| 对比 | 膨胀模式 | 自适应模式 |
|------|----------|-----------|
| 适用场景 | 文字、线条、连续图案 | 离散圆点、光斑 |
| 算法 | 形态膨胀 + 连通区域质心 | 距离变换 + 局部极大值 |
| 精度 | 中等（质心可能偏离视觉中心） | 高（精确检测圆点中心） |
| 依赖 | 纯 numpy | 需要 scipy |
| 参数 | iterations, structure | 自动估算，无需手动调整 |

### Q4: 为什么 Y 轴固定为 0？

图像是 2D 的，算法将其映射为 XZ 平面上的点云。Y 轴（垂直方向）固定为 0，后续可通过其他操作符（如起飞、编队变换）将无人机提升到空中。

### Q5: 可以同时导入多张图像吗？

可以，每次执行操作符导入一张图像。每张图像会创建独立的网格对象和参考对象。

### Q6: 缩放后的点云间距一定是 min_distance 吗？

缩放保证**最接近的两架无人机**间距等于 `min_distance`。其他无人机对的间距可能更大，取决于原始图像的像素分布。
