# SkybrushNebulaOperator 交接文档

## 1. 概述

**类名**: `SkybrushNebulaOperator` **文件路径**: `src/modules/sbstudio/plugin/operators/hhang_operators.py`

**功能**: 计算星云模式的起飞和降落轨迹。通过冲突检测算法为每架无人机规划不碰撞的飞行路径，支持原地起飞（in situ）和编队起飞两种模式。这是**最复杂的操作符**，使用贝塞尔曲线生成平滑轨迹并通过冲突检测确保无人机间安全距离。

***

## 2. 类属性

| 属性               | 值                                      |
| ---------------- | -------------------------------------- |
| `bl_idname`      | `skybrush.nebula`                      |
| `bl_label`       | Nebula                                 |
| `bl_description` | Calculating nebula takeoff and landing |
| `bl_options`     | `REGISTER`, `UNDO`（支持撤销）               |

### 2.1 参数

#### 帧范围参数

| 参数              | 类型          | 默认值 | 范围 | 说明               |
| --------------- | ----------- | --- | -- | ---------------- |
| `takeoff_frame` | IntProperty | 1   | ≥1 | 无人机开始起飞的帧        |
| `shape_frame`   | IntProperty | -   | -  | 星云编队的初始帧（默认为当前帧） |

#### 安全参数

| 参数                    | 类型            | 默认值 | 范围    | 说明         |
| --------------------- | ------------- | --- | ----- | ---------- |
| `distance`            | FloatProperty | 3m  | 1~50m | 无人机间最小间距   |
| `height`              | FloatProperty | 50m | ≥10m  | 虚拟空中降落位置高度 |
| `speed_change_height` | FloatProperty | 10m | 1~20m | 在此高度改变速度   |

#### 速度参数

| 参数            | 类型            | 默认值    | 说明     |
| ------------- | ------------- | ------ | ------ |
| `xy_velocity` | FloatProperty | 10 m/s | 水平最大速度 |
| `z_velocity`  | FloatProperty | 2 m/s  | 垂直最大速度 |

#### 计算参数

| 参数           | 类型          | 默认值      | 范围 | 说明                       |
| ------------ | ----------- | -------- | -- | ------------------------ |
| `complexity` | IntProperty | 20181213 | ≥1 | 每轮并行检测的无人机数量（默认无人机数×10%） |

#### 模式参数

| 参数        | 类型           | 默认值   | 说明                               |
| --------- | ------------ | ----- | -------------------------------- |
| `takeoff` | BoolProperty | False | True=起飞模式（时间倒推），False=降落模式（时间正推） |
| `insitu`  | BoolProperty | False | 原地起飞模式（不重新分配目标）                  |

***

## 3. 核心逻辑

### 3.1 执行流程

```
invoke() → 设置 shape_frame 和 complexity 默认值，弹出参数对话框
    ↓
execute() → 开始执行
    ↓
1. 获取起飞位置（takeoff_frame 帧）和编队目标位置（shape_frame 帧）
2. 生成贝塞尔曲线轨迹（考虑水平/垂直速度和下降阶段）
3. 根据模式选择无人机分配策略：
   - insitu: 按距离中心的远近排序分配
   - 默认: 贪婪分组，每次选最远对开始
4. 使用冲突检测算法计算每架无人机的延迟时间
5. 逐架插入关键帧（起始 → 目标 → 下降阶段）
6. 起飞模式下在 takeoff_frame 插入额外关键帧
```

### 3.2 贝塞尔曲线生成

#### 控制点定义

```python
bezier_knots = np.array([(0, 0), (1/3, 0), (2/3, 1), (1, 1)])
```

使用三次贝塞尔曲线，控制点为：

* P₀ = (0, 0) — 起点

* P₁ = (1/3, 0) — 保持水平

* P₂ = (2/3, 1) — 上升到顶

* P₃ = (1, 1) — 终点

#### 轨迹生成

```python
def gen_point(p1, p2, t):
    b = sum(comb(3, i) * t**i * (1-t)**(3-i) * bezier_knots[i] for i in range(4))[1]
    return np.multiply(np.array(p2) - p1, (b, b, t)) + p1

def gen_trajectory(p1, p2):
    trajectory = [p1]
    distance = np.sqrt((np.subtract(p1, p2) ** 2).sum())
    frames = math.ceil(max(distance * 1.5 / self.xy_velocity,
                           abs(p1[2] - p2[2]) / self.z_velocity) * fps)
    trajectory.extend([gen_point(p1, p2, i) for i in np.linspace(0, 1, frames + 1)[1:]])
    for h, f in landing:
        diff = h - p2[2]
        trajectory.extend([(p2[0], p2[1], p2[2] + diff * i / f) for i in range(1, f + 1)])
        p2 = (p2[0], p2[1], h)
    return frames, np.array(trajectory, dtype=np.float64)
```

**帧数计算**: 取水平距离×1.5/xy_velocity 和 垂直距离/z_velocity 中的较大值，确保足够的时间完成飞行。

#### 下降阶段

```python
landing, height = [], self.height
if height > self.speed_change_height:
    landing.append((self.speed_change_height, math.ceil((height - self.speed_change_height) * fps / 2)))
    height = self.speed_change_height
landing.append((0, math.ceil(height * fps)))
```

| 阶段   | 目标高度                | 帧数                                         |
| ---- | ------------------- | ------------------------------------------ |
| 减速阶段 | speed_change_height | `(height - speed_change_height) × FPS / 2` |
| 降落阶段 | 0                   | `speed_change_height × FPS`                |

### 3.3 无人机分配策略

#### 原地模式（insitu=True）

```python
order = np.argsort((np.subtract(shape, np.mean(targets, axis=0)) ** 2).sum(-1))
trajectories = [(drones[i], targets[i],
                 *gen_trajectory(get_position_of_object(drones[i]), targets[i]))
                    for i in order]
```

按无人机当前位置到编队中心（targets 的平均位置）的距离由近到远排序，然后一一配对。适用于无人机已在目标位置附近的情况。

#### 编队模式（insitu=False）

```python
def find_farthest_pair(drones_positions, targets):
    a = np.array(drones_positions)
    b = np.array(targets)
    i = np.min(np.sum((a - b[:,None,:]) ** 2, axis=-1), axis=1).argmax()
    return (np.argmin(((a - b[i]) ** 2).sum(-1)), i)

def find_nearest(group, targets):
    a = np.array([t for _, t in group])
    b = np.array(targets)
    c = np.min(np.sqrt(np.sum((a - b[:,None,:]) ** 2, axis=-1)), axis=1)
    for i in np.argsort(c):
        if c[i] >= self.distance: return i
    return None

groups = []
while len(drones):
    si, ti = find_farthest_pair(shape, targets)
    group = [(drones[si], targets[ti])]
    del(drones[si]); del(shape[si]); del(targets[ti])
    while len(drones):
        ti = find_nearest(group, targets)
        if ti is None: break
        si = np.argmin(((np.array(shape) - targets[ti]) ** 2).sum(-1))
        group.append((drones[si], targets[ti]))
        del(drones[si]); del(shape[si]); del(targets[ti])
    groups.append(group)
```

**算法说明**:

1. **选最远对**: 找到距离其最近目标最远的无人机，将其与最近目标配对作为组的起点

2. **贪婪扩展**: 每次选与当前组中所有目标距离都 ≥ `distance` 的无人机加入组

3. **组内处理**: 确保组内无人机目标间距满足安全距离

4. 重复直到所有无人机都被分配

### 3.4 冲突检测

#### 工具函数

```python
from sbstudio.plugin.utils.evaluator import check_distance, check_trajectory
```

| 函数                                                   | 功能                                 |
| ---------------------------------------------------- | ---------------------------------- |
| `check_distance(traj1, point, distance_sq)`          | 检查轨迹 traj1 与单个点 point 是否全程保持安全距离   |
| `check_trajectory(traj1, traj2, delay, distance_sq)` | 检查 traj1 延迟 delay 帧后是否与 traj2 发生碰撞 |

#### 延迟计算（核心算法）

```python
def delay(frame_current, trajectory, runnings):
    for i in itertools.count(0):
        for traj in runnings:
            if not check_trajectory(trajectory, traj, i, distance_sq):
                break
        else:
            return i
```

**算法说明**:

1. 从 0 开始遍历递增的延迟帧数 `i`

2. 对每个 `i`，检查轨迹与所有运行中轨迹（runnings）是否冲突

3. 第一个不冲突的 `i` 即为最小延迟

### 3.5 主循环

```python
with ConsoleWindow():
    start, runnings, frame_current, total = time.time(), [], self.shape_frame, len(trajectories)
    move = (lambda f, n: f - n) if self.takeoff else (lambda f, n: f + n)
    while trajectories:
        N = np.inf
        for i in range(min(len(trajectories), self.complexity)):
            traj = trajectories[i][3]
            for j in range(len(trajectories)):
                if i != j and not check_distance(traj, trajectories[j][3][0], distance_sq):
                    break
            else:
                if (n := delay(frame_current, traj, runnings)) < N and (N := n, I := i)[0] == 0:
                    break
        if N == np.inf:
            N, I = delay(frame_current, trajectories[0][3], runnings), 0
        drone, target, frames, trajectory = trajectories[I]
        trajectories.pop(I)
        frame_current = move(frame_current, N)
        runnings = [t[N:] for t in runnings]
        runnings = [t for t in runnings if t.shape[0]] + [trajectory]
        keyframe_insert(drone, frame_current)
        drone.location, frame = target, move(frame_current, frames)
        keyframe_insert(drone, frame)
        for height, frames in landing:
            drone.location[2], frame = height, move(frame, frames)
            keyframe_insert(drone, frame)
        if self.takeoff:
            keyframe_insert(drone, self.takeoff_frame)
```

**算法流程**:

1. **选择最佳无人机**: 在 `complexity` 限制内，找到延迟最小的无人机

2. **更新状态**:

   * 移除已处理的无人机

   * 前进当前帧 `frame_current += N`（降落）或 `-= N`（起飞）

   * 裁剪运行中轨迹 `runnings = [t[N:] for t in runnings]`

   * 添加新轨迹到运行列表

3. **插入关键帧**:

   * 起始位置（`frame_current`）

   * 目标位置（`frame_current + frames`）

   * 下降阶段各高度点

   * 起飞模式额外在 `takeoff_frame` 插入关键帧

**进度日志**:

```
星云[2018]: 已用时5.2s 进度45.67%
```

### 3.6 关键帧插入

```python
def keyframe_insert(drone, frame, interpolation=("BEZIER", "BEZIER", "LINEAR")):
    drone.keyframe_insert(data_path="location", frame=frame)
    for i in range(3):
        set_interpolation(drone, frame, i, interpolation[i])

def set_interpolation(drone, frame, index, interpolation):
    kp = drone.animation_data.action.fcurves.find("location", index=index).keyframe_points
    for k in [k for k in kp if k.co[0] == frame]:
        k.interpolation = interpolation
```

默认插值方式 `("BEZIER", "BEZIER", "LINEAR")`：

* X/Y 轴：BEZIER（平滑曲线）

* Z 轴：LINEAR（匀速升降）

***

## 4. 数据结构

### 4.1 输入

| 变量        | 类型           | 说明                    |
| --------- | ------------ | --------------------- |
| `drones`  | list[Object] | 无人机对象列表               |
| `targets` | list[tuple]  | 目标位置列表 (x, y, height) |
| `shape`   | list[tuple]  | 编队初始位置列表              |
| `context` | Context      | Blender 上下文           |

### 4.2 轨迹元组

```python
(drone, target, frames, trajectory_array)
```

| 字段                 | 类型       | 说明                     |
| ------------------ | -------- | ---------------------- |
| `drone`            | Object   | 无人机对象                  |
| `target`           | tuple    | 目标位置 (x, y, height)    |
| `frames`           | int      | 到达目标所需帧数               |
| `trajectory_array` | np.array | NumPy 数组，包含所有路径点 (N×3) |

### 4.3 运行轨迹列表

```python
runnings = [trajectory_array_1, trajectory_array_2, ...]
```

每处理一架无人机后，将其轨迹添加到运行列表中。每次迭代时裁剪已过去的部分 `runnings = [t[N:] for t in runnings]`。

***

## 5. 验证结果展示

本操作符执行后直接在时间线中生成关键帧动画。可通过以下方式验证：

1. **3D 视图**：播放时间线，观察星云起飞/降落效果

2. **Dope Sheet**：查看每架无人机的位置关键帧分布（错开分布）

3. **Graph Editor**：查看 F-Curve 插值方式

4. **控制台日志**：查看计算进度和用时

***

## 6. 依赖关系

### 6.1 外部依赖

| 模块                                      | 用途                 |
| --------------------------------------- | ------------------ |
| `bpy`                                   | Blender Python API |
| `numpy`                                 | 数组和矩阵运算            |
| `math`                                  | 数学函数（阶乘、ceil）      |
| `itertools`                             | 无限计数迭代             |
| `time`                                  | 计时功能               |
| `sbstudio.plugin.constants.Collections` | 查找无人机集合            |
| `sbstudio.plugin.utils.evaluator`       | 获取无人机位置、冲突检测函数     |
| `sbstudio.api.console`                  | 控制台输出窗口            |

### 6.2 相关代码

| 文件                                   | 关系                                          |
| ------------------------------------ | ------------------------------------------- |
| `sbstudio/plugin/utils/evaluator.py` | `check_distance`, `check_trajectory` 冲突检测函数 |

***

## 7. 调试与日志

### 7.1 日志输出

```python
print(f"\r星云[{self.complexity}]: 已用时{time.time() - start:.1f}s 进度{percent:.2f}%", end="")
```

进度日志实时显示在 Blender 系统控制台，包含：

* 复杂度参数

* 已用时间（秒）

* 完成百分比

### 7.2 调试验证方法

1. 在小规模无人机数量（<50）上测试，观察计算时间

2. 检查关键帧分布是否合理错开

3. 播放动画检查是否有碰撞

4. 查看控制台日志确认计算进度

### 7.3 性能特征

| 无人机数量 | 预计时间 |
| ----- | ---- |
| 50    | 几秒   |
| 200   | 几十秒  |
| 1000  | 几分钟  |

计算复杂度约为 O(n² × complexity)，主要耗时在冲突检测循环。

### 7.4 常见问题排查

| 问题        | 可能原因                     |
| --------- | ------------------------ |
| 计算时间过长    | 无人机数量过多或 complexity 设置过大 |
| 关键帧重叠     | distance 参数设置过小          |
| 起飞/降落方向反了 | takeoff 参数设置错误           |

***

## 8. 常见问题 FAQ

### Q1: takeoff 和降落模式的区别是什么？

| 对比项     | 起飞模式 (takeoff=True) | 降落模式 (takeoff=False) |
| ------- | ------------------- | -------------------- |
| 时间方向    | 倒推（帧号递减）            | 正推（帧号递增）             |
| move 函数 | `f - n`             | `f + n`              |
| 额外关键帧   | 在 takeoff_frame 插入  | 无                    |
| 用途      | 从地面起飞到编队            | 从编队降落到地面             |

### Q2: insitu 模式适用于什么场景？

`insitu=True` 时无人机原地起飞，按距离编队中心由近到远分配目标。适用于：

* 无人机已在目标位置附近

* 不需要重新分配起飞顺序

* 希望保持原有编队结构

### Q3: complexity 参数的作用是什么？

`complexity` 控制每轮并行冲突检测的无人机数量：

* 值越大，每轮检查的无人机越多，找到的最优延迟越准确

* 值越小，计算越快但可能不是最优解

* 默认值为无人机总数 × 10%

对于大规模无人机（>1000），可以适当增大 complexity 提高解的质量。

### Q4: 为什么使用贝塞尔曲线而不是直线？

贝塞尔曲线提供平滑的加速度变化，避免突然的速度跳变：

* 水平方向：先慢后快再慢，模拟真实起飞过程

* 垂直方向：线性上升，保持匀速

* 视觉上更加自然流畅

### Q5: 下降阶段的作用是什么？

下降阶段模拟无人机到达目标后的安全降落过程：

1. 先在 `speed_change_height` 高度减速

2. 然后匀速降落到地面

这适用于降落模式，确保无人机安全着地。
