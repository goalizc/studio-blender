# 物体内部检测函数 交接文档

## 1. 概述

文件路径: `src/modules/sbstudio/plugin/model/light_effects.py`

灯光特效系统中需要判断无人机是否在某个网格物体内部（`target = "INSIDE_MESH"`），文件包含两个实现：

| 函数 | 行数 | 说明 |
|------|------|------|
| [`test_containment`](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/model/light_effects.py#L123-L161) | 123-161 | 修复版本，基于射线法准确判断点是否在封闭网格内部 |
| [`test_containment_skybrush`](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/model/light_effects.py#L163-L182) | 163-182 | 原始版本，存在检测不准确的问题 |

当前系统使用 `test_containment`（见 [第1329行](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/model/light_effects.py#L1329)）。

---

## 2. test_containment_skybrush 原始版本

### 2.1 代码

```python
def test_containment_skybrush(bvh_tree: BVHTree | None, point: Coordinate3D) -> bool:
    """Given a point and a BVH-tree, tests whether the point is _probably_
    within the mesh represented by the BVH-tree.

    This is done by casting three rays in the X, Y and Z directions. The point
    is assumed to be within the mesh if all three rays hit the mesh.

    Returns True if the BVH-tree is missing.
    """
    if not bvh_tree:
        return True

    for axis in CONTAINMENT_TEST_AXES:
        _, _, _, dist = bvh_tree.ray_cast(point, axis)
        if dist is None or dist == -1:
            return False

    return True
```

### 2.2 算法原理

沿 X、Y、Z 三个方向各发射一条射线，如果**三条射线都命中网格**，则认为点在内部。

### 2.3 存在问题

| 问题 | 说明 |
|------|------|
| **误判凹形物体** | 射线穿过凹形物体时可能命中多个面，但算法只检查是否命中，不统计次数 |
| **误判甜甜圈形状** | 点在甜甜圈孔洞中时，三个方向都可能命中外侧网格，但点实际在外部 |
| **误判薄壁物体** | 点在薄壁物体外部但靠近表面时，射线可能恰好命中，导致误判为内部 |
| **文档注明 "probably"** | 原作者也意识到这是近似检测，非精确判断 |

### 2.4 误判示例

```
    ┌─────┐
    │  ·  │  ← 点在空心立方体内部，三方向都命中 → 正确判定为内部
    └─────┘

    ┌─┐ ┌─┐
    │ · │    ← 点在两个分离立方体之间，
    └─┘ └─┘    X方向命中两侧 → 误判为内部

    ○          ← 点在甜甜圈孔洞中，
                 X/Y方向命中外环 → 误判为内部
```

---

## 3. test_containment 修复版本

### 3.1 代码

```python
def test_containment(bvh_tree: Optional[BVHTree], point: Coordinate3D) -> bool:
    # 定义射线方向（任意方向都可以，这里选择X轴正方向）
    direction = Vector((1, 0, 0))

    # 进行射线投射
    hit, normal, index, distance = bvh_tree.ray_cast(point, direction)

    # 如果没有命中任何面，点在物体外部
    if hit is None:
        return False

    # 计算射线与网格的交点数量
    intersections = 0
    max_distance = 10000.0  # 设定一个足够大的最大距离

    current_point = point
    current_distance = 0.0

    while True:
        # 从当前点继续发射射线
        hit, normal, index, dist = bvh_tree.ray_cast(current_point, direction)

        # 如果没有更多交点，退出循环
        if hit is None:
            break

        # 累加交点数量
        intersections += 1

        # 更新当前点位置（稍微向前移动以避免浮点数精度问题）
        current_distance += dist + 0.01
        current_point = Vector(point) + direction * current_distance

        # 防止无限循环
        if current_distance > max_distance:
            break

    # 如果交点数量为奇数，点在内部；偶数则在外部
    return (intersections % 2 == 1)
```

### 3.2 算法原理：射线法 (Ray Casting)

基于**奇偶规则** (Odd-Even Rule)：

> 从点 P 沿任意方向发射一条射线，统计射线与网格的交点数量：
> - **奇数** → 点在内部
> - **偶数** → 点在外部

**数学原理**: 每次射线穿过网格表面，点在内部/外部状态就切换一次。从外部开始（状态 = 外部），穿过奇数次后到达内部，穿过偶数次后回到外部。

### 3.3 执行流程

```
输入: BVH树, 检测点
    ↓
1. 沿 X+ 方向发射第一条射线
    ↓
2. 未命中 → 点在外部，返回 False
    ↓
3. 命中 → 进入循环统计交点
    ↓
4. 累加交点数量 (intersections += 1)
    ↓
5. 前进到交点之后 (dist + 0.01)
    ↓
6. 继续发射射线
    ↓
7. 无更多交点 或 超过最大距离 → 退出循环
    ↓
8. 返回 intersections % 2 == 1
```

### 3.4 关键设计

#### 避免浮点数精度问题

```python
current_distance += dist + 0.01
current_point = Vector(point) + direction * current_distance
```

每次前进时额外偏移 `0.01`，避免射线起点恰好落在网格表面上导致重复命中同一面。

#### 防止无限循环

```python
max_distance = 10000.0
if current_distance > max_distance:
    break
```

对于复杂的自相交网格或螺旋结构，设置最大距离限制。

#### 方向选择

```python
direction = Vector((1, 0, 0))  # X轴正方向
```

选择任意方向均可，X+ 方向简单且通常有效。特殊情况下（射线恰好平行于某个面）可通过 0.01 偏移规避。

---

## 4. 两个版本对比

### 4.1 准确性对比

| 场景 | test_containment_skybrush | test_containment |
|------|---------------------------|------------------|
| 封闭凸形物体 | ✅ 正确 | ✅ 正确 |
| 封闭凹形物体 | ❌ 可能误判 | ✅ 正确 |
| 空心物体（孔洞中） | ❌ 误判为内部 | ✅ 正确判定为外部 |
| 分离物体之间 | ❌ 可能误判 | ✅ 正确 |
| 点在表面上 | ⚠️ 不确定 | ⚠️ 可能受浮点影响 |

### 4.2 性能对比

| 指标 | test_containment_skybrush | test_containment |
|------|---------------------------|------------------|
| 射线数量 | 固定 3 条 | 1 + N 条（N = 交点数） |
| 时间复杂度 | O(1) | O(N)，N 通常较小 |
| 最坏情况 | 3 次 BVH 查询 | 网格面片数次查询 |

### 4.3 适用场景

| 版本 | 适用场景 |
|------|----------|
| `test_containment_skybrush` | 快速粗略检测，对精度要求不高 |
| `test_containment` | 精确检测，需要正确判断点是否在封闭网格内部 |

---

## 5. 在系统中的应用

### 5.1 调用位置 [`_get_spatial_effect_predicate`](file:///e:/hhang/studio-blender/src/modules/sbstudio/plugin/model/light_effects.py#L1326-L1339)

```python
def _get_spatial_effect_predicate(self) -> Callable[[Coordinate3D], bool] | None:
    if self.target == "INSIDE_MESH":
        bvh_tree = self._get_bvh_tree_from_mesh()
        func = partial(test_containment, bvh_tree)  # ← 使用修复版本
    elif self.target == "FRONT_SIDE":
        plane = self._get_plane_from_mesh()
        func = partial(test_is_in_front_of, plane)
    else:
        func = None
```

### 5.2 使用场景

| 灯光特效配置 | 效果 |
|-------------|------|
| `target = "INSIDE_MESH"` | 仅对网格内部的无人机应用特效 |
| `mesh` = 球形网格 | 球内无人机变色，球外不变 |
| `mesh` = 建筑模型 | 建筑内部无人机有特效，外部无 |

---

## 6. 算法限制

### 6.1 已知限制

| 限制 | 说明 |
|------|------|
| **要求封闭网格** | 算法假设网格是封闭的，开口网格会导致检测不准确 |
| **自相交网格** | 网格自相交时，交点计数可能不准确 |
| **点在表面上** | 射线起点恰好落在表面上时，结果不确定 |
| **单方向射线** | 仅沿 X+ 方向，如果网格在该方向有特殊情况（如所有面平行于 X 轴），可能漏检 |

### 6.2 改进建议

如需更高精度，可考虑：
1. **多方向射线**: 沿多个方向发射射线，取多数投票结果
2. **符号距离场 (SDF)**: 预计算网格的符号距离场，O(1) 查询
3. ** winding number 算法**: 更精确的点包含检测，支持非流形网格

---

## 7. 依赖关系

### 7.1 外部依赖

| 模块 | 用途 |
|------|------|
| `mathutils.Vector` | 3D 向量运算 |
| `mathutils.bvhtree.BVHTree` | BVH 树结构，支持射线查询 |

### 7.2 内部依赖

| 函数 | 用途 |
|------|------|
| `_get_bvh_tree_from_mesh` | 从网格物体获取 BVH 树 |
| `_get_spatial_effect_predicate` | 创建空间特效谓词函数 |

---

## 8. 调试验证方法

### 8.1 在 Python 控制台测试

```python
from sbstudio.plugin.model.light_effects import test_containment, test_containment_skybrush
from mathutils.bvhtree import BVHTree
from mathutils import Vector
import bpy

# 获取物体的 BVH 树
obj = bpy.data.objects["MyMesh"]
depsgraph = bpy.context.evaluated_depsgraph_get()
tree = BVHTree.FromObject(obj, depsgraph)

# 测试点
test_points = [
    Vector((0, 0, 0)),       # 中心点
    Vector((10, 0, 0)),      # 外部点
]

for point in test_points:
    result_old = test_containment_skybrush(tree, point)
    result_new = test_containment(tree, point)
    print(f"点 {point}: 原始={result_old}, 修复={result_new}")
```

### 8.2 常见问题排查

| 问题 | 可能原因 |
|------|----------|
| 所有点都判定为外部 | BVH 树为空或网格无面片 |
| 所有点都判定为内部 | 网格未封闭或网格法线反转 |
| 结果不稳定 | 点在表面上，浮点精度影响 |

---

## 9. 常见问题 FAQ

### Q1: 为什么选择 X+ 方向而不是其他方向？

任意方向在数学上都是等效的。X+ 方向是最简单的选择。0.01 的额外偏移可以处理射线恰好平行于网格面的情况。

### Q2: 0.01 的偏移量是怎么确定的？

这是一个经验值，需要大于浮点数精度误差（约 1e-6），但小于网格的最小特征尺寸。对于大多数无人机编队场景（尺度 1-1000m），0.01 是一个合理的选择。

### Q3: 如果网格有开口怎么办？

开口网格会导致射线逃逸，从而错误地将内部点判定为外部。这种情况下建议：
1. 使用封闭网格
2. 或改用 `FRONT_SIDE` 模式（基于平面检测）

### Q4: 为什么原始版本要用三个方向？

原作者试图通过多方向检测提高准确性，但方法本身有根本缺陷：即使三个方向都命中，也不能证明点在内部（如甜甜圈孔洞的情况）。修复版本通过统计交点数量从根本上解决了这个问题。
