# 编队过渡匹配算法 交接文档

## 1. 概述

**文件路径**: `src/modules/sbstudio/api/algorithm.py`

**功能**: 这是**编队过渡的核心算法**，负责将起飞位置与目标编队位置进行最优匹配，使得所有无人机沿直线飞行时的最小间距最大化，从而避免碰撞。算法使用**匈牙利算法初始化 + 模拟退火局部搜索**的组合优化策略。

---

## 2. 核心函数

### 2.1 函数概览

| 函数 | 行数 | 功能 |
|------|------|------|
| `trajectory_min_distance_vectorized` | 15-18 | 向量化计算多条轨迹对之间的最小距离 |
| `max_min_distance_matcher_internal` | 20-77 | 核心优化算法（模拟退火 + 局部搜索） |
| `max_min_distance_matcher` | 79-84 | 公开接口，处理数量不等的情况 |

---

## 3. 算法详解

### 3.1 `trajectory_min_distance_vectorized` — 轨迹最小距离计算

```python
def trajectory_min_distance_vectorized(a1, b1, a2, b2):
    u, v = a1 - a2, (b1 - a1) - (b2 - a2)
    n, d = np.einsum('nd,nd->n', u, v), np.einsum('nd,nd->n', v, v)
    return np.linalg.norm(u + np.clip(-n / (d + 1e-10), 0.0, 1.0)[:, None] * v, axis=1)
```

**数学原理**: 计算两条线段 `a1→b1` 和 `a2→b2` 之间的最短距离。

**公式推导**:
- `u = a1 - a2`: 起点差向量
- `v = (b1-a1) - (b2-a2)`: 方向差向量
- 两条线段上任意两点可表示为: `P(t) = a1 + t·(b1-a1)`, `Q(t) = a2 + t·(b2-a2)`
- 距离函数: `D(t) = ||P(t) - Q(t)|| = ||u + t·v||`
- 最小值在 `t* = -(u·v) / (v·v)` 处取得（投影法）
- 由于 `t ∈ [0,1]`，使用 `clip(t*, 0, 1)` 约束范围

**向量化**: 一次计算 N 条轨迹对的距离，避免循环。

---

### 3.2 `max_min_distance_matcher_internal` — 核心优化算法

这是**整个算法最复杂的部分**，结合了多种优化策略。

#### 3.2.1 初始化阶段

```python
np.random.seed(20181213)
critical = np.asarray([0.8, 0.95, 1.0]) * threshold
weights = np.asarray([0.8, 0.5, 0.2])
N, start = len(A), time()
dist_matrix, perm = np.full((N, N), np.inf), linear_sum_assignment(distance_matrix(A, B) ** 3)[1]
```

| 参数 | 值 | 说明 |
|------|-----|------|
| `critical` | `[0.8T, 0.95T, 1.0T]` | 接受率阈值分界点（T=安全距离） |
| `weights` | `[0.8, 0.5, 0.2]` | 对应各分界点的接受概率 |
| `perm` | 匈牙利算法结果 | 初始匹配方案 |

**匈牙利算法初始化**: 使用 `linear_sum_assignment(distance_matrix(A, B) ** 3)` 找到最小距离和的初始匹配。三次方放大远距离差异，优先匹配距离近的点对。

```python
closest_B = np.argsort(distance_matrix(B, B), axis=1)[:, 1:]
i, j = np.triu_indices(N, k=1)
dist_matrix[i, j] = trajectory_min_distance_vectorized(A[i], B[perm[i]], A[j], B[perm[j]])
Perm, Min = perm.copy(), (current_min := np.min(dist_matrix))
```

- `closest_B`: 预计算 B 中每个点的最近邻排序，用于快速找到替代目标
- 初始化上三角距离矩阵：计算每对无人机轨迹的最小距离
- `Perm`, `Min`: 记录全局最优解和最小距离

#### 3.2.2 迭代优化阶段

```python
for count in itertools.count():
    acceptance_rate = np.interp(Min, critical, weights)
    n = int(np.ceil(acceptance_rate * N / 2))
    if Min >= threshold:
        break
```

**接受率计算**: 根据当前最小距离 `Min` 使用线性插值计算接受率：

| 当前最小距离 | 接受率 | 含义 |
|------------|--------|------|
| `Min < 0.8T` | 0.8 | 接受劣解概率高，探索性强 |
| `Min = 0.95T` | 0.5 | 中等接受率 |
| `Min ≥ T` | 0.2 | 接受率最低，快速收敛 |

```python
i, j = np.unravel_index(flat_index := np.argmin(dist_matrix), dist_matrix.shape)
if s != flat_index:
    s, e = flat_index, e - 1 if e > 0 else 0
elif e < 9:
    e += 1
elif k < n:
    i, k = np.where(perm == closest_B[perm[j]][k])[0][0], k + 1
else:
    break
```

**邻居选择策略**（三重机制）:

| 步骤 | 条件 | 动作 | 目的 |
|------|------|------|------|
| 1 | 最小距离对位置变化 | 重置探索计数 `e=0` | 新方向优先 |
| 2 | `e < 9` | 增加探索计数 | 在当前对附近搜索 |
| 3 | `k < n` | 选择 `B[perm[j]]` 的第 `k` 个最近邻 | 广度搜索替代目标 |
| 4 | 都不满足 | 终止迭代 | 无法进一步优化 |

```python
new_dist_matrix, new_perm, new_min = generate_neighbor(i, j)
if new_min > current_min or np.random.random() < acceptance_rate:
    dist_matrix, perm, current_min = new_dist_matrix, new_perm, new_min
    if current_min > Min:
        Perm, Min, e, k = perm.copy(), current_min, 0, 1
```

**接受准则**（模拟退火）:
- **必接受**: `new_min > current_min`（改进解）
- **概率接受**: `random() < acceptance_rate`（劣解，避免局部最优）
- **更新全局最优**: 当找到更好的最小距离时，重置探索状态

#### 3.2.3 `generate_neighbor` — 邻居生成

```python
def generate_neighbor(i, j):
    new_dist_matrix = dist_matrix.copy()
    new_perm = perm.copy()
    new_perm[i], new_perm[j] = new_perm[j], new_perm[i]

    k_indices = np.arange(N)
    a2_k = A[k_indices]
    b2_k = B[new_perm[k_indices]]
    a1_i = np.tile(A[i], (N, 1))
    b1_i = np.tile(B[new_perm[i]], (N, 1))
    distances_i = trajectory_min_distance_vectorized(a1_i, b1_i, a2_k, b2_k)
    a1_j = np.tile(A[j], (N, 1))
    b1_j = np.tile(B[new_perm[j]], (N, 1))
    distances_j = trajectory_min_distance_vectorized(a1_j, b1_j, a2_k, b2_k)

    for k in range(N):
        if k != i: new_dist_matrix[(k, i) if k < i else (i, k)] = distances_i[k]
        if k != j: new_dist_matrix[(k, j) if k < j else (j, k)] = distances_j[k]

    return new_dist_matrix, new_perm, np.min(new_dist_matrix)
```

**算法**:
1. 交换 `perm[i]` 和 `perm[j]`（交换两个无人机的目标）
2. **增量更新**距离矩阵：只重新计算与 `i` 和 `j` 相关的轨迹对，避免 O(N²) 全量计算
3. 返回更新后的距离矩阵和新排列

---

### 3.3 `max_min_distance_matcher` — 公开接口

```python
def max_min_distance_matcher(A, B, *, threshold=2.5):
    if len(A) == len(B):
        return max_min_distance_matcher_internal(A, B, threshold=threshold)
    base = linear_sum_assignment(distance_matrix(A, B) ** 3)[1]
    perm, dist = max_min_distance_matcher_internal(A, B[base], threshold=threshold)
    return base[perm].tolist(), dist
```

**数量不等处理**:
1. 当 A 和 B 数量不同时，先用匈牙利算法找到 B 的最佳子集 `base`
2. 对 A 和 `B[base]` 调用核心算法
3. 映射回原始索引 `base[perm]`

---

## 4. 返回值

```python
return Perm.tolist(), dist.tolist()
```

| 返回值 | 类型 | 说明 |
|--------|------|------|
| `perm` | list[int] | 匹配方案：`perm[i]` 表示 A 中第 i 个点匹配到 B 中的第 `perm[i]` 个点 |
| `dist` | list[float] | `[xy最大距离, z下降最大距离, z上升最大距离]` |

---

## 5. 算法使用位置

### 5.1 调用链

```
recalculate_transitions.py (操作符)
    ↓
SkybrushStudioAPI.match_points() [base.py:661-669]
    ↓
max_min_distance_matcher() [algorithm.py:79-84]
    ↓
max_min_distance_matcher_internal() [algorithm.py:20-77]
```

### 5.2 `SkybrushStudioAPI.match_points` [base.py:661-669]

```python
def match_points(self, source, target, *, radius=None):
    threshold = bpy.context.scene.skybrush.safety_check.proximity_warning_threshold + 0.5
    return max_min_distance_matcher(target, source, threshold=threshold)
```

**安全距离**: 从场景设置中读取 `proximity_warning_threshold`（默认 2.5m），并加 0.5m 作为缓冲。

### 5.3 `SkybrushStudioAPI.plan_transition` [base.py:807-830]

```python
def plan_transition(self, source, target, *, max_velocity_xy, max_velocity_z, ...):
    threshold = bpy.context.scene.skybrush.safety_check.proximity_warning_threshold + 0.5
    perm, (xydist, zdowndist, zupdist) = max_min_distance_matcher(target, source, threshold=threshold)
    zdowndist, zupdist = -min(0, -zdowndist), max(0, -zupdist)
    duration = xydist * 1.5 / max_velocity_xy
    duration = max(duration, zdowndist * 1.5 / max_velocity_z)
    duration = max(duration, zupdist * 1.5 / (max_velocity_z_up or max_velocity_z))
    return TransitionPlan(start_times=[0]*len(target), durations=[duration]*len(target), mapping=perm, clearance=None)
```

**持续时间计算**: 根据匹配结果的最大距离和速度约束，计算所有无人机完成过渡所需的时间。取水平、下降、上升三个方向的最大值。

### 5.4 `calculate_mapping_for_transition_into_storyboard_entry` [recalculate_transitions.py:254-308]

```python
if entry.transition_type == "AUTO":
    if not entry.mapping.startswith("*"):
        target = get_coordinates_of_formation(formation, frame=entry.frame_start)
        match = get_api().match_points(source, target, radius=0.0)[0]
```

**故事板过渡**: 当故事板条目设置为 `AUTO` 过渡类型且未使用自定义映射（`*` 前缀）时，调用此算法计算无人机与目标点的匹配。

---

## 6. 依赖关系

### 6.1 外部依赖

| 模块 | 用途 |
|------|------|
| `numpy` | 数组运算、向量化计算 |
| `scipy.spatial.distance_matrix` | 计算点集间的距离矩阵（优先使用） |
| `scipy.optimize.linear_sum_assignment` | 匈牙利算法（优先使用） |
| `sbstudio.api.munkres.Munkres` | 匈牙利算法（scipy 不可用时的后备方案） |
| `itertools` | 无限计数迭代 |
| `time` | 计时功能 |

### 6.2 降级策略

```python
try:
    from scipy.spatial import distance_matrix
    from scipy.optimize import linear_sum_assignment
except:
    from sbstudio.api.munkres import Munkres
    distance_matrix = lambda a, b: np.linalg.norm(a[:, np.newaxis] - b, axis=2)
    linear_sum_assignment = lambda M: np.asarray(Munkres().compute(M))
```

当 scipy 不可用时，使用内置的 Munkres 算法实现和自定义距离矩阵计算。

### 6.3 相关代码

| 文件 | 关系 |
|------|------|
| `sbstudio/api/base.py` | `match_points()` 和 `plan_transition()` 调用此算法 |
| `sbstudio/plugin/operators/recalculate_transitions.py` | 故事板过渡计算入口 |
| `sbstudio/api/munkres.py` | 匈牙利算法的纯 Python 实现 |

---

## 7. 调试与日志

### 7.1 日志输出

```python
print(f"\r\033[Ktime: {time() - start:.3f}, count: {count}, min: {Min:.3f}, jump: {k}/{n}", end="")
```

| 字段 | 说明 |
|------|------|
| `time` | 已用时间（秒） |
| `count` | 迭代次数 |
| `min` | 当前最小轨迹距离 |
| `jump` | 当前探索步数/最大探索步数 |

### 7.2 性能特征

| 无人机数量 | 预计时间 |
|------------|----------|
| 50 | <1 秒 |
| 200 | 几秒 |
| 1000 | 几十秒 |

计算复杂度约为 O(N² × count)，主要耗时在距离矩阵更新和邻居生成。

### 7.3 调试验证方法

1. 在 Blender 控制台中观察迭代进度日志
2. 检查 `Min` 是否达到 `threshold`（≥2.5m）
3. 在 3D 视图中播放动画，观察过渡是否平滑无碰撞
4. 使用 Safety Check 面板验证最小距离

### 7.4 常见问题排查

| 问题 | 可能原因 |
|------|----------|
| 迭代次数过多 | `threshold` 设置过高，无人机过于密集 |
| 最小距离不达标 | 初始编队或目标编队内部间距过小 |
| 计算时间过长 | 无人机数量过多（>1000） |

---

## 8. 算法特点

### 8.1 优势

| 特点 | 说明 |
|------|------|
| **增量更新** | `generate_neighbor` 只更新 O(N) 距离，而非 O(N²) 全量计算 |
| **模拟退火** | 接受劣解避免陷入局部最优 |
| **自适应接受率** | 根据当前解质量动态调整探索/利用平衡 |
| **匈牙利初始化** | 提供良好的初始解，加速收敛 |
| **三重邻居策略** | 深度优先 + 广度搜索结合，平衡探索效率 |

### 8.2 局限性

| 局限 | 说明 |
|------|------|
| **仅直线轨迹** | 算法只考虑直线过渡，不支持曲线轨迹 |
| **无时间调度** | 所有无人机同时开始和结束过渡，不支持错开起飞 |
| **O(N²) 内存** | 距离矩阵占用 O(N²) 内存，大规模时可能成为瓶颈 |
| **确定性随机** | 固定随机种子 `20181213`，结果可复现但无法多次采样 |

---

## 9. 常见问题 FAQ

### Q1: 为什么使用 `distance_matrix(A, B) ** 3` 作为匈牙利算法的代价？

三次方放大远距离的差异，使得算法更倾向于匹配距离近的点对。如果直接用距离（一次方），近距离点的优势不够明显，可能导致整体匹配不佳。

### Q2: 接受率是如何计算的？

使用 `np.interp(Min, critical, weights)` 进行线性插值：

```
Min < 0.8T  →  接受率 = 0.8
Min = 0.9T  →  接受率 ≈ 0.65（插值）
Min = 0.95T →  接受率 = 0.5
Min = T     →  接受率 = 0.2
```

### Q3: 为什么固定随机种子 `20181213`？

确保结果可复现。相同的输入总是产生相同的匹配方案，便于调试和版本控制。

### Q4: `closest_B` 的作用是什么？

预计算 B 中每个点的最近邻排序。当交换 `perm[i]` 和 `perm[j]` 无法改善时，算法尝试将 `i` 分配到 `B[perm[j]]` 的最近邻，加速搜索过程。

### Q5: 终止条件有哪些？

1. `Min >= threshold`: 达到目标安全距离，最优情况
2. `k >= n` 且 `e >= 9`: 探索步数耗尽，局部搜索完成
3. 无法找到更好的邻居，接受率趋近于 0
